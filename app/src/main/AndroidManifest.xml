<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.freeteam01.androidnewsgroupreader">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_opt"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_opt"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        android:screenOrientation="portrait">


        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SubscribeActivity" android:label="@string/subscribe"
            android:theme="@style/Base.V21.Theme.AppCompat.Light" />
        <activity android:name=".PostActivity" android:label="Post"
            android:theme="@style/Base.V21.Theme.AppCompat.Light" />
        <activity android:name=".LoginActivity" android:label="@string/title_activity_login"
            android:theme="@style/AppTheme" />
        <activity android:name=".RegisterActivity" android:label="@string/title_activity_register"
            android:theme="@style/AppTheme" />
        <activity
            android:name="com.microsoft.windowsazure.mobileservices.authentication.RedirectUrlActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:host="easyauth.callback" android:scheme="fakenewsapptestapp" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AddArticleActivity"
            android:theme="@style/Theme.Transparent"
            android:label="" />
    </application>

</manifest>